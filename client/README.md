# Client

This folder contains the React + Vite frontend for the Finance Dashboard.

For full installation and run instructions, use the root README:

- [../README.md](../README.md)

Frontend-only commands:

```bash
npm run dev
npm run build
npm run lint
```

The development server runs on `http://localhost:5173` and proxies `/api` requests to `http://localhost:3001`.
